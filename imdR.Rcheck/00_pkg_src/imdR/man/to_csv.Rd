% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{to_csv}
\alias{to_csv}
\title{Extract a daily time series at a point location}
\usage{
to_csv(imd_raster, lat, lon, file_path = NULL)
}
\arguments{
\item{imd_raster}{A terra SpatRaster from get_data().}

\item{lat}{Latitude in decimal degrees (WGS84).}

\item{lon}{Longitude in decimal degrees (WGS84).}

\item{file_path}{Character or NULL. If provided, saves output as CSV.}
}
\value{
An invisible data frame with columns date and value.
}
\description{
Extracts daily values from an IMD SpatRaster at the nearest grid
cell to the specified latitude/longitude and returns a data frame.
}
\examples{
\donttest{
r  <- get_data("rain", 2020, 2020, tempdir())
df <- to_csv(r, lat = 15.5, lon = 73.8)
head(df)

# Save directly to file
to_csv(r, lat = 15.5, lon = 73.8,
       file_path = file.path(tempdir(), "panaji_rain_2020.csv"))
}
}
