% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/point.R
\name{get_points}
\alias{get_points}
\title{Extract daily time series for one variable at many points}
\usage{
get_points(
  points,
  variable,
  start_yr,
  end_yr,
  file_dir,
  format = "long",
  download = TRUE,
  dedup = TRUE,
  save_csv = TRUE
)
}
\arguments{
\item{points}{Data frame with columns lat, lon, optionally name.}

\item{variable}{One of "rain", "tmax", "tmin".}

\item{start_yr}{Start year.}

\item{end_yr}{End year.}

\item{file_dir}{Directory for .grd files.}

\item{format}{"long" or "wide". Default "long".}

\item{download}{Use get_data() if TRUE, open_data() if FALSE. Default TRUE.}

\item{dedup}{Collapse points sharing a grid cell? Default TRUE.}

\item{save_csv}{Save output as CSV? Default TRUE.}
}
\value{
Invisible data frame in the requested format.
}
\description{
Reads the raster once and extracts all locations in a single pass
with grid-cell deduplication. Far faster than looping get_point()
for many coordinates.
}
\examples{
\donttest{
pts <- data.frame(name = c("Panaji", "Margao"),
                  lat = c(15.49, 15.28), lon = c(73.83, 73.99))
df  <- get_points(pts, "rain", 2020, 2020,
                  file_dir = tempdir(), format = "wide")
}
}
